{
  "name": "agent-awareness-plugins",
  "version": "0.1.1",
  "description": "Community provider plugins for agent-awareness",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --import tsx --test --test-concurrency=1 tests/*.test.ts"
  },
  "workspaces": [
    "github-watcher",
    "server-health",
    "pr-pilot",
    "claim-debugger"
  ],
  "devDependencies": {
    "@types/node": "^25.5.0",
    "agent-awareness": "^0.3.0",
    "tsx": "^4.21.0"
  }
}
